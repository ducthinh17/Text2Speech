/** @type {import('next').NextConfig} */
const nextConfig = {
  // Các tùy chọn cấu hình Next.js của bạn ở đây
  images: {
    domains: ["lh3.googleusercontent.com"], // Thêm hostname cho hình ảnh
  },
};

module.exports = nextConfig;
